import React from 'react';
import { View, Image, SafeAreaView } from 'react-native';
import { StatusBarIcons, WifiIcon, SignalIcon, BatteryIcon } from './StatusBarIcons';
import { MicrophoneInput } from './MicrophoneInput';
import { HelpButton } from './HelpButton';

const InputDesign = () => {
  return (
    <SafeAreaView className="flex-1 bg-white">
      {/* Status Bar */}
      <View className="h-11 px-4 flex-row justify-between items-center">
        <View className="flex-row space-x-2">
          <SignalIcon />
          <WifiIcon />
        </View>
        <BatteryIcon />
      </View>

      {/* Main Content */}
      <View className="flex-1 items-center justify-center relative">
        {/* Bus Image */}
        <Image
          source={{ uri: "https://cdn.builder.io/api/v1/image/assets/TEMP/ae01930140f5bc36c356635a1e6a4b90a9897ecd?placeholderIfAbsent=true" }}
          className="w-full h-40 mb-8"
          resizeMode="contain"
          accessibilityLabel="bus"
        />

        {/* Microphone Input */}
        <MicrophoneInput />

        {/* Help Button */}
        <HelpButton />
      </View>
    </SafeAreaView>
  );
};

export default InputDesign;